Mismo ejemplo. Con la diferencia que ahora se envia un conjunto de scripts de entrenamiento en lugar de un solo script.

Para hacer esto se debe generar un package con el conjunto de scripts o una imagen docker. Esto debe hacerlo el usuario que envia los jobs
de entrenamiento y no es automático como cuando se enviaba un solo script de entrenamiento